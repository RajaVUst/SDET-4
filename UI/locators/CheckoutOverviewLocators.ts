export class CheckoutOverviewLocators {

    static readonly checkoutPage =
        '[data-testid="checkout-page"]';

    static readonly guestName =
        '[data-testid="guest-name-input"]';

    static readonly guestEmail =
        '[data-testid="guest-email-input"]';

    static readonly guestPhone =
        '[data-testid="guest-phone-input"]';

    static readonly street =
        '[data-testid="shipping-street-input"]';

    static readonly city =
        '[data-testid="shipping-city-input"]';

    static readonly state =
        '[data-testid="shipping-state-select"]';

    static readonly zip =
        '[data-testid="shipping-zip-input"]';

    static readonly country =
        '[data-testid="shipping-country-input"]';

    static readonly continueButton =
        '[data-testid="continue-to-payment-button"]';

    static readonly total =
        '[data-testid="checkout-total"]';

}